# Runtime dependencies for the Vercel serverless deployment.
# The `credit_agent` package is imported via PYTHONPATH=src (see vercel.json)
# and the sys.path insert in vercel_app.py — no editable install needed.
fastapi
starlette
pydantic
pandas
numpy
openpyxl
reportlab
python-docx
httpx
tavily-python
python-multipart
